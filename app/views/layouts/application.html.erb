<!DOCTYPE html>
<html>
<head>
  <title>Rukaba</title>
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>

	[
	<% @boards.each do |board| %>
		<% if board == @boards.first %>
			<a href="/<%= board.url %>/" title="<%= board.desc %>"><%= board.url %></a>
		<% else %>
			/
			<a href="/<%= board.url %>/" title="<%= board.desc %>"><%= board.url %></a>
		<% end %>
	<% end %>
	]

<%= yield %>


	[
	<% @boards.each do |board| %>
		<% if board == @boards.first %>
			<a href="/<%= board.url %>/" title="<%= board.desc %>"><%= board.url %></a>
		<% else %>
			/
			<a href="/<%= board.url %>/" title="<%= board.desc %>"><%= board.url %></a>
		<% end %>
	<% end %>
	]
	
	
	
</body>
</html>
